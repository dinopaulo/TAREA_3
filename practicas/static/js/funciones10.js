function tabla_mult_3(){
    const multiplicador = 3;
    var multiplicando = 1;
    while(multiplicando <= 12){
      var resultado = multiplicador * multiplicando;
      var msg = multiplicador + " x " + multiplicando +" = " + resultado;
      alert(msg);
      multiplicando = multiplicando + 1;
      
    }
  }
 
  